# Copy this file to .env and fill in your actual local IP.
# .env itself should NOT be committed (it's already in .gitignore) since it's
# specific to your machine's network — .env.example is the committed template.

EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10/firesight-api
